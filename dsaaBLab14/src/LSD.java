public class LSD {
    //consider characters from right to left
    //stable sort,key index counting is stable

    public static void sort(String []a, int w){
        int R = 256;
        int N = a.length;
        String [] aux = new String[N];

        for (int d = w-1; d >=0 ; d--) {
            
        }


    }
}
